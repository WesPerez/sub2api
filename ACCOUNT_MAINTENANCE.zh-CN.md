# 应用内账号维护与备注同步

## 定时开放账号

管理员在「账号管理 → 更多操作 → 定时开放账号」配置 AgentRouter 恢复任务。
默认暂停；切换时先停止旧 `sub2api-recovery.timer` 并等待原执行结束，再在此处开启。
计划、时区、类型显示名称、名称后缀别名、每类开放数量和记录保留均由本应用保存。
默认 GPT、Claude、GLM、DeepSeek 各 3 个，每小时整点按有效余额降序、账号 ID 升序选择。
结构化余额默认有效 6 小时，可配置为 1 至 168 小时；只有从未接入结构化同步的账号暂时沿用旧备注。
已接入账号的快照损坏、凭据变更或过期时保持原开关，不能用旧备注恢复失效余额。
未知后缀、人工停用账号和没有有效余额候选的整类保留原状态，并显示原因。

每次编辑先「核对账号」再保存。修改配置不会立即执行；手动执行单独确认，使用已保存配置。
新增类型先填写后缀，可以暂时同时保留新旧名称别名。当前站点固定为已核实的 `agentrouter.org`。
应用停止时取消任务，错过整点不补跑。每次执行的开始、结束、结果、错误与实际开关数量持久化。
多实例共享数据库会话锁、每个计划时间和手动请求 ID 有唯一执行记录；不宣称调度天然只发生一次。

独立的策略、编排、仓储和 HTTP/页面模块复用现有账号状态服务。先开放入选账号，全部成功后再关闭
同类落选账号。每个变更通过数据库原子条件核对身份与状态，并在同一语句写入缓存失效 outbox；
身份在预览后变化的账号会跳过。任务不改平台、分组、账号 ID、凭据或消费历史。

## 跨应用备注 API

MetAPI 等调用方只配置本应用管理 API 地址与凭证，不读取 PostgreSQL、不接触部署目录。
所有端点沿用管理员鉴权和操作审计：

| 请求 | 用途 |
| --- | --- |
| `GET /api/v1/admin/account-notes?page=1&page_size=100` | 按 ID 分页读取 API-key 账号的匹配字段、备注和版本 |
| `GET /api/v1/admin/account-notes/:id` | 读取一个账号，响应同时携带 `ETag` |
| `PUT /api/v1/admin/account-notes/:id` | `If-Match` 为上次版本；同时原子写入 `notes` 和可选 `balance_snapshot` |

返回匹配字段 `id/name/platform/type/base_url/notes/notes_revision`、`balance_state` 和公开 `balance_snapshot`，
不返回上游密钥或内部身份哈希。快照含版本、来源、目标身份、来源账号编号、十进制余额、额度和 UTC 采样时间。
写入输入仅为 `source_account_id/amount/quota/observed_at`；目标身份由 Sub2API 校验并绑定。
来源编号属于发布者自己的命名空间，用于追溯，不是跨库外键。MetAPI 依据受配置的一对一别名与站点匹配，
允许已认证发布者在自身账号重新编号后提交更新采样；Sub2API 不把另一项目的局部数据库 ID 固化为永久身份。
快照时间最多允许未来 60 秒；旧采样及同时间不同内容返回冲突。无快照的 notes-only 请求保留已存余额。
版本覆盖备注及账号业务身份，瞬时限流和最后使用时间不引发无谓冲突。
写入在数据库内原子比较；账号改名、换平台、换凭据或备注变化均返回 `412`，不会覆盖新的内容。
缺少版本返回 `428`，非法版本返回 `400`。不接受 `*` 或弱 ETag。
调用方收到冲突应保留现值并跳过本轮；超时不盲目重发，也不降级到旧无条件写接口。
整点任务重新生成执行计划，提交前再次校验身份、余额快照和状态。并发刷新与修改会保守跳过受影响账号，
保留现有开关并记录原因；不以未经核实的缓存余额重试。
必须先发布提供此 API 的 Sub2API，再发布依赖它的 MetAPI。

## 验证范围

CI 覆盖：持久化执行槽、全局锁、配置 CAS、空类型保护、余额排序、enable-before-disable、
账号身份变化、备注读写间的竞争、敏感字段投影、HTTP 前置条件与存储错误分类。
生产启用前只读比较旧规则与新预览；真实执行后核对持久化结果和旧执行器已停用。

旧 Python 恢复器和已停用的余额直写、代理批量对齐任务已退役。生产 bridge 故障修复需要在应用停止时运行，
由服务器调度仓库的 `tasks/sub2api-network-attachment/` 管理，采用 Docker 事件与低频核对兜底。
